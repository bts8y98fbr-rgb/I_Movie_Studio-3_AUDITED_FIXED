# AI Movie Studio Studio Edition

Windows desktop AI movie studio. Development follows `PROJECT_SPEC.md` and `TASK.md`.

## Phase 1

The current foundation provides configuration, JSON settings, SQLite storage, project management and application logging.

Run tests with:

```bash
python -m pytest
```
